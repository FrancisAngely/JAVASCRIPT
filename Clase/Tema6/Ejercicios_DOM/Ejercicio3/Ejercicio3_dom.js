const colores=document.getElementById("Colores"); 
const txt= document.getElementById("txt");
const boton_anadir= getElementById("anadir");
const boton_eliminar= getElementById("eliminar");

/*document.getElementById("addItemButton").addEventListener("click", function () {
  const itemInput = document.getElementById("itemInput");
  const itemSelect = document.getElementById("itemSelect");

  const newItem = itemInput.value.trim();

  if (newItem) {
    const option = document.createElement("option");
    option.value = newItem;
    option.textContent = newItem;
    itemSelect.appendChild(option);
    itemInput.value = ""; // Limpiar el cuadro de texto
  } else {
    alert("Por favor, introduce un ítem.");
  }
});

document
  .getElementById("removeItemButton")
  .addEventListener("click", function () {
    const itemSelect = document.getElementById("itemSelect");

    if (itemSelect.options.length > 1) {
      // Verificamos que haya ítems para eliminar
      itemSelect.remove(itemSelect.options.length - 1); // Eliminar el último ítem
    } else {
      alert("No hay ítems para eliminar.");
    }
  });*/
